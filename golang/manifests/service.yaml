apiVersion: v1
kind: Service
metadata:
  name: math
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "math"
    port: 8080
  selector:
    app: math
