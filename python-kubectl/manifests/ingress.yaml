apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: vote
  annotations:
    kubernetes.io/ingress.class: nginx  
spec:
  rules:
  - host: vote-pchico83.cloud.okteto.net
    http:
      paths:
      - path: /
        backend: 
          serviceName: vote
          servicePort: 8080
  tls:
  - hosts:
    - vote-pchico83.cloud.okteto.net